import { ExprFloat, PrimitiveFloat } from "./expr";
import { Float } from "../exprtypes";
/** true depth expression */
export declare class TrueDepthExpr extends ExprFloat {
    depth: Float;
    constructor(depth: Float);
    /** sets the distance to convert to the true depth */
    setDist(depth: PrimitiveFloat | number): void;
}
/** calculates the linear depth from inverse depth value `1 / distance` */
export declare function truedepth(depth: Float | number): TrueDepthExpr;
