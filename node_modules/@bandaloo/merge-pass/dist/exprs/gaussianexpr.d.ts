import { Float } from "../exprtypes";
import { ExprFloat, PrimitiveFloat } from "./expr";
/** gaussian expression */
export declare class GaussianExpr extends ExprFloat {
    x: Float;
    a: Float;
    b: Float;
    constructor(x: Float, a: Float, b: Float);
    setX(x: PrimitiveFloat | number): void;
    setA(a: PrimitiveFloat | number): void;
    setB(b: PrimitiveFloat | number): void;
}
/**
 * gaussian function that defaults to normal distribution
 * @param x x position in the curve
 * @param a horizontal position of peak (defaults to 0 for normal distribution)
 * @param b horizontal stretch of the curve (defaults to 1 for normal distribution)
 */
export declare function gaussian(x: Float | number, a?: Float | number, b?: Float | number): GaussianExpr;
