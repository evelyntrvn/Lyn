import { Float } from "../exprtypes";
import { EffectLoop } from "../mergepass";
import { PrimitiveFloat } from "./expr";
/** bloom loop */
export declare class BloomLoop extends EffectLoop {
    threshold: Float;
    horizontal: Float;
    vertical: Float;
    boost: Float;
    constructor(threshold?: Float, horizontal?: Float, vertical?: Float, boost?: Float, samplerNum?: number, taps?: 5 | 9 | 13, reps?: number);
    /**
     * set the horizontal stretch of the blur effect (no greater than 1 for best
     * effect)
     */
    setHorizontal(num: PrimitiveFloat | number): void;
    /**
     * set the vertical stretch of the blur effect (no greater than 1 for best
     * effect)
     */
    setVertical(num: PrimitiveFloat | number): void;
    /** set the treshold */
    setThreshold(num: PrimitiveFloat | number): void;
    /** set the contrast boost */
    setBoost(num: PrimitiveFloat | number): void;
}
/**
 * creates a bloom loop
 * @param threshold values below this brightness don't get blurred (0.4 is
 * about reasonable, which is also the default)
 * @param horizontal how much to blur vertically (defaults to 1 pixel)
 * @param vertical how much to blur horizontally (defaults to 1 pixel)
 * @param taps how many taps for the blur (defaults to 9)
 * @param reps how many times to loop the blur (defaults to 3)
 */
export declare function bloom(threshold?: Float | number, horizontal?: Float | number, vertical?: Float | number, boost?: Float | number, samplerNum?: number, taps?: 5 | 9 | 13, reps?: number): BloomLoop;
