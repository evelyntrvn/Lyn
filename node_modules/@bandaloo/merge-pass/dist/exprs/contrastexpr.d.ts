import { Float, Vec4 } from "../exprtypes";
import { ExprVec4, PrimitiveFloat } from "./expr";
export declare class ContrastExpr extends ExprVec4 {
    contrast: Float;
    constructor(contrast: Float, col?: Vec4);
    /** sets the contrast */
    setContrast(contrast: PrimitiveFloat | number): void;
}
/**
 * changes the contrast of a color
 * @param val float for how much to change the contrast by (should probably be
 * between -1 and 1)
 * @param col the color to increase the contrast of (defaults to current
 * fragment color)
 */
export declare function contrast(val: Float | number, col?: Vec4): ContrastExpr;
