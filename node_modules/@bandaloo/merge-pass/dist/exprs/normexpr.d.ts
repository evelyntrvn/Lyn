import { Vec } from "../exprtypes";
import { Operator } from "./expr";
/** normalize expression */
export declare class NormExpr<T extends Vec> extends Operator<T> {
    vec: T;
    constructor(vec: T);
    /** sets the vec to normalize */
    setVec(vec: T): void;
}
/** creates an expression that normalizes a vector */
export declare function norm<T extends Vec>(vec: T): NormExpr<T>;
