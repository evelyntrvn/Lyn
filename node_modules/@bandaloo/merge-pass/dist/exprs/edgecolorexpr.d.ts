import { Vec4 } from "../exprtypes";
import { WrappedExpr, PrimitiveVec4, ExprVec4 } from "./expr";
/** edge color expression */
export declare class EdgeColorExpr extends WrappedExpr<Vec4> {
    color: Vec4;
    expr: ExprVec4;
    constructor(color: Vec4, samplerNum?: number, stepped?: boolean);
    setColor(color: PrimitiveVec4): void;
}
/**
 * creates a colored edge detection expression
 * @param color what color to make the edge
 * @param samplerNum where to sample from
 * @param stepped whether to round the result of sobel edge detection (defaults
 * to true)
 */
export declare function edgecolor(color: Vec4, samplerNum?: number, stepped?: boolean): EdgeColorExpr;
