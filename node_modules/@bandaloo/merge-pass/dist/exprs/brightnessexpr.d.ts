import { Float, Vec4 } from "../exprtypes";
import { ExprVec4, PrimitiveFloat } from "./expr";
/** brightness expression */
export declare class Brightness extends ExprVec4 {
    brightness: Float;
    constructor(brightness: Float, col?: Vec4);
    /** set the brightness (should probably be between -1 and 1) */
    setBrightness(brightness: PrimitiveFloat | number): void;
}
/**
 * changes the brightness of a color
 * @param val float for how much to change the brightness by (should probably be
 * between -1 and 1)
 * @param col the color to increase the brightness of (defaults to current
 * fragment color)
 */
export declare function brightness(val: Float | number, col?: Vec4): Brightness;
