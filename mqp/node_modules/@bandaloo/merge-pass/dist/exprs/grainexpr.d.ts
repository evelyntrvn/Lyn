import { Float } from "../exprtypes";
import { ExprVec4, PrimitiveFloat } from "./expr";
/** grain expression */
export declare class GrainExpr extends ExprVec4 {
    grain: Float;
    constructor(grain: Float);
    /** sets the grain level  */
    setGrain(grain: PrimitiveFloat | number): void;
}
/**
 * creates an expression that adds random grain
 * @param val how much the grain should impact the image (0 to 1 is reasonable)
 */
export declare function grain(val: Float | number): GrainExpr;
