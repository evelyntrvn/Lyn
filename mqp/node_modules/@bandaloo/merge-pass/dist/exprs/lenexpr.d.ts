import { Vec } from "../exprtypes";
import { ExprFloat } from "./expr";
/** length expression */
export declare class LenExpr<T extends Vec> extends ExprFloat {
    vec: T;
    constructor(vec: T);
    setVec(vec: T): void;
}
/** creates an expreession that calculates the length of a vector */
export declare function len<T extends Vec>(vec: T): LenExpr<T>;
